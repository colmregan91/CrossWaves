using System.Collections;
using UnityEngine;

public class RunTimeStartUp : MonoBehaviour
{
    // TODO: download missing crossword files from backend on startup
    // e.g. check persistentDataPath for each expected file, download any that are missing
}